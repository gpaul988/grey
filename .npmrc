# cPanel npm configuration — prevent OOM on shared hosting
# ============================================================================

# Reduce parallelism
maxsockets=1

# Avoid peer dependency resolution issues on low-memory systems
legacy-peer-deps=false

# Cache optimization
prefer-offline=true

# Production install flags
omit=dev
