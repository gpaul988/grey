// STICKY IMAGE FIX - Replace the right section div around line 520 with this:

// OLD (broken):
// <div className='lg:sticky lg:top-[6em] justify-center items-center lg:h-screen overflow-hidden lg:block md:block hidden'>

// NEW (fixed - truly sticky viewport):
// The trick: Use fixed positioning for the actual image container, 
// then absolute position it within a relative parent that spans the service section

// Key changes:
// 1. Wrap image in a viewport-fixed container
// 2. Only show it while scrolling through service section
// 3. Position updates via scroll listener (already in code via activeId)
// 4. Container uses fixed + hidden when scrolling past services
